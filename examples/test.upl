// Test UPLim file for engine analysis

func main() -> Int {
  let x = 42
  let name = "UPLim"
  
  // This will trigger a warning
  let value = null
  
  say("Hello from UPLim!")
  
  return 0
}

type Point = {
  x: Int,
  y: Int
}

func distance(p1: Point, p2: Point) -> Float {
  let dx = p1.x - p2.x
  let dy = p1.y - p2.y
  return sqrt(dx * dx + dy * dy)
}

// TODO: Add more functions
