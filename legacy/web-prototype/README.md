# Historical Web Prototype

This directory contains the former root-level Next.js application that experimented with a playground, engine dashboard, and browser-facing CLI helpers.

It is preserved for reference only:

- It is not part of the active `pnpm` workspace.
- It still depends on older engine/compiler interfaces.
- Active web development happens in `apps/website`.
