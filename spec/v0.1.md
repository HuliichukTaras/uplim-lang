# UPLim v0.1 Spec Pointer

The frozen `v0.1` source of truth now lives in:

- `/docs/uplim-core-specification.md`

Supporting grammar artifacts live in:

- `/syntax/tree-sitter/grammar.js`
- `/spec/v0.1.ebnf`

This file exists only as a compatibility pointer so the repository no longer carries two competing `v0.1` specifications.
